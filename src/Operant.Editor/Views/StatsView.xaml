<UserControl x:Class="Operant.Editor.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="12">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Style="{StaticResource ToolbarText}"
                     Text="{Binding StatsFilter, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="Show all counters (incl. quest flags &amp; timestamps)"
                      VerticalAlignment="Center" Margin="16,0,0,0"
                      IsChecked="{Binding ShowAllCounterGroups}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding CountersView}">
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Background="#EFEFEF" Margin="0,2,0,2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Margin="6,2"/>
                                <TextBlock Text="(" Foreground="#888" Margin="4,2,0,2"/>
                                <TextBlock Text="{Binding ItemCount}" Foreground="#888" Margin="0,2"/>
                                <TextBlock Text=")" Foreground="#888" Margin="0,2,6,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Counter"
                                    Binding="{Binding DisplayKey}"
                                    IsReadOnly="True"
                                    Width="*"/>
                <DataGridTextColumn Header="Value"
                                    Binding="{Binding Value, UpdateSourceTrigger=LostFocus}"
                                    Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
