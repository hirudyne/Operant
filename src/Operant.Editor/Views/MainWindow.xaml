<Window x:Class="Operant.Editor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Operant.Editor.ViewModels"
        xmlns:views="clr-namespace:Operant.Editor.Views"
        Title="{Binding Title}"
        Height="640" Width="880"
        MinHeight="480" MinWidth="640">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.InputBindings>
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding OpenCommand}"/>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveCommand}"/>
        <KeyBinding Key="S" Modifiers="Control+Shift" Command="{Binding SaveAsCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open..."     InputGestureText="Ctrl+O" Command="{Binding OpenCommand}"/>
                <MenuItem Header="_Save"        InputGestureText="Ctrl+S" Command="{Binding SaveCommand}"/>
                <MenuItem Header="Save _As..."  InputGestureText="Ctrl+Shift+S" Command="{Binding SaveAsCommand}"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="ExitClick"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="_About..." Click="AboutClick"/>
            </MenuItem>
        </Menu>

        <Border DockPanel.Dock="Bottom" Style="{StaticResource StatusBar}">
            <TextBlock Text="{Binding Status}"/>
        </Border>

        <Grid>
            <TextBlock Text="Open a save file via File → Open... or press Ctrl+O."
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="#888" FontSize="14"
                       Visibility="{Binding HasSave, Converter={StaticResource BoolToCollapsed}}"/>

            <TabControl Margin="6"
                        Visibility="{Binding HasSave, Converter={StaticResource BoolToVisible}}">
                <TabItem Header="Overview">
                    <views:OverviewView/>
                </TabItem>
                <TabItem Header="Inventory">
                    <views:InventoryView/>
                </TabItem>
                <TabItem Header="Stats">
                    <views:StatsView/>
                </TabItem>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>
